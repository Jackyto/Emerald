<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/RelativeLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.emerald.MainActivity$PlaylistFragment" >

    <Button
        android:id="@+id/newPlaylistFragButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_margin="4dp"
        android:background="@drawable/background_color_inv"
        android:text="@string/newPlaylist"
        android:textColor="@drawable/text_selector_inv" />

    <ExpandableListView
        android:id="@+id/playlistView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/newPlaylistFragButton"
        android:layout_margin="2dp"
        android:background="@drawable/background_color_inv"
        android:childDivider="#00000000" >

    </ExpandableListView>

</RelativeLayout>